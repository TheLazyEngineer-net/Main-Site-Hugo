[build]
  publish = "public/"
  command = 'hugo --gc --minify && npx pagefind@latest --site "public"'
  [build.environment]
    HUGO_VERSION = "0.152.2"
    HUGO_ENV = "production"
    TZ = "America/Chicago"
